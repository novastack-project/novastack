name: Testing

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-common:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies for novastack-common
        working-directory: ./novastack-common
        run: uv sync --extra dev

      - name: Run tests for novastack-common
        working-directory: ./novastack-common
        run: uv run pytest -v --tb=short

  test-instrumentation:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies for novastack-instrumentation
        working-directory: ./novastack-instrumentation
        run: uv sync --extra dev

      - name: Run tests for novastack-instrumentation
        working-directory: ./novastack-instrumentation
        run: uv run pytest -v --tb=short

  # test-core:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       python-version: ["3.11"]
    
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Install uv
  #       uses: astral-sh/setup-uv@v6

  #     - name: Set up Python ${{ matrix.python-version }}
  #       run: uv python install ${{ matrix.python-version }}

  #     - name: Install dependencies for novastack-core
  #       working-directory: ./novastack-core
  #       run: uv sync --extra dev

  #     - name: Run tests for novastack-core
  #       working-directory: ./novastack-core
  #       run: uv run pytest -v --tb=short
