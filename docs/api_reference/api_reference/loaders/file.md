=== "pip"

    ```bash
    $ pip install novastack-loaders-file
    ```

=== "uv"

    ```bash
    $ uv add novastack-loaders-file
    ```

::: novastack.loaders.file
    options:
        inherited_members: true
        filters:
        - "!^_"
        - "!class_name"
