=== "pip"

    ```bash
    $ pip install novastack-loaders-docling
    ```

=== "uv"

    ```bash
    $ uv add novastack-loaders-docling
    ```

::: novastack.loaders.docling
    options:
        inherited_members: true
        filters:
        - "!^_"
        - "!class_name"
