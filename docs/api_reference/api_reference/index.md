# Novastack

Build production-ready AI applications with modular, composable components.

---

## Installation

=== "pip"

    ```bash
    $ pip install novastack
    ```

=== "uv"

    ```bash
    $ uv add novastack
    ```
